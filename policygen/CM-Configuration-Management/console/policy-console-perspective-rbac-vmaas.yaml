apiVersion: operator.openshift.io/v1
kind: Console
metadata:
  name: cluster
spec:
  managementState: Managed
  customization:
    perspectives:
      # Platform administrators
      - id: admin
        visibility:
          accessReview:
            required:
              - group: config.openshift.io
                resource: clusteroperators
                verb: list
          state: AccessReview

      # Tenant container workloads — portal marker + not a platform admin
      - id: dev
        visibility:
          accessReview:
            missing:
              - group: config.openshift.io
                resource: clusteroperators
                verb: list
            required:
              - group: ""
                resource: configmaps
                verb: get
                namespace: tenancies
                name: portal-developer
          state: AccessReview

      # Hub-local Virtualization — platform admins only
      - id: virtualization-perspective
        visibility:
          accessReview:
            required:
              - group: config.openshift.io
                resource: clusteroperators
                verb: list
          state: AccessReview

      # Fleet Management — platform operators only (tenants use VMaaS plugin perspective)
      - id: acm
        visibility:
          accessReview:
            required:
              - group: config.openshift.io
                resource: clusteroperators
                verb: list
          state: AccessReview
